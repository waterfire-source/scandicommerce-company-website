# ===========================================
# Sanity CMS Configuration
# ===========================================

# Get these from https://www.sanity.io/manage
# Create a new project or use an existing one

# Your Sanity project ID (found in sanity.io/manage)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id

# Dataset name (usually 'production' or 'development')
NEXT_PUBLIC_SANITY_DATASET=production

# API version (use date format: YYYY-MM-DD)
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Optional: Token for authenticated requests (for drafts/preview)
# Create this at sanity.io/manage -> API -> Tokens
SANITY_API_READ_TOKEN=your_read_token

# ===========================================
# Existing Environment Variables
# ===========================================

# Add any other environment variables your project needs here
